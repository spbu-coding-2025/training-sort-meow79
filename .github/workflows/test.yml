name: Build and test

on:
  push:
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build source code
        run: |
          ./scripts/build.sh
          ./scripts/test.sh
        timeout-minutes: 1
